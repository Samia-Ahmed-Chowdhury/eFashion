/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["images.unsplash.com","plus.unsplash.com","picsum.photos","unsplash.com"]
    }
}

module.exports = nextConfig